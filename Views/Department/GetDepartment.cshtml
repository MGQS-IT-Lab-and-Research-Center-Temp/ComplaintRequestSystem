@model ComplaintRequestSystem.Models.Department.DepartmentViewModel
@{
    ViewData["Title"] = "Department Detail";
}

<div class="body">

    @if (Model is not null)
    {
        <dl class="row">
            <dt class="col-sm-4">
                Department Name:
            </dt>

            <dd class="col-sm-8">
                @Model.Name
            </dd>

            <dt class="col-sm-4">
                Description:
            </dt>

            <dd class="col-sm-8">
                @Model.Description
            </dd>
        </dl>
        <br />
        <div class="btn-group">
            <a asp-controller="Department" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-success">Update</a>
            &nbsp;
            <form asp-action="DeleteDepartment" asp-route-id="@Model.Id">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
            &nbsp;
            <a asp-controller="Department" asp-action="Index" class="btn btn-outline-secondary">Go back to list</a>
        </div>
    }
</div>
